apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: shukra-living
spec:
  type: NodePort
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 30011
    targetPort: 30011
    nodePort: 30011
